## 1. Wording

- [x] 1.1 In `src/index.html`, change the title-block label to `Save to`, the transport buttons to `Start transcribing` and `Stop transcribing`, the status label to `Not transcribing`, and the live chart's empty state to `Nothing transcribed yet.` / `Press <strong>Start transcribing</strong> to capture this meeting.` Verify that the file-side strings ("Drop recordings", "No recording loaded", "the start of the recording") are untouched.
- [x] 1.2 In `src/main.js`, update `STATE_LABEL`: `idle` to `Not transcribing`, `advancing` to `Transcribing`, `penlift` to `Transcribing stalled — no output`. Leave `loaded`, `listening` and `stopping` as they are. Verify with `node --check src/main.js`.
- [x] 1.3 In `src/main.js`, change the log marker `recording started` to `transcription started`. Verify that the other markers (`stop requested`, `engine exited unexpectedly`) are unchanged.
- [x] 1.4 Update the one line in `DESIGN.md` that quotes the primary button's label. Then verify the whole sweep: `grep -rn -i record src/index.html src/main.js DESIGN.md` lists only the deliberate leftovers from design.md's Non-Goals — the file-side strings, the CSS class names, the chart-recorder metaphor and the "Record lift" token — and nothing from the live flow.
- [x] 1.5 Check it in the running GUI (`cargo tauri dev`): the button reads "Start transcribing" and the transport row still fits on one line; starting a session switches the status through "Transcribing"; stopping shows "Stop transcribing"; the engine log's first marker reads "transcription started"; and the live chart's empty state reads "Nothing transcribed yet."

  **Verified by the user 2026-09-11 (`cargo tauri dev`, Fedora/KDE): "All passed."** The button reads "Start transcribing" and the transport row still fits, the status moves through "Transcribing", the stop button reads "Stop transcribing", the engine log's first marker reads "transcription started", the live empty state reads "Nothing transcribed yet.", and the title block reads "Save to".
