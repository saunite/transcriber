## 1. Mark source tasks as moved

- [x] 1.1 In `openspec/changes/remove-installer-packaging/tasks.md`, mark tasks 2.6, 4.2, 4.3, 5.1, and the Windows portion of 5.4 as `[x]` with a "Superseded — moved to `05-remove-installer-packaging-windows`" note, following the existing convention used at that file's task 6.4

  Already done — this annotation was applied proactively before `remove-installer-packaging` was archived (see `openspec/changes/archive/2026-09-03-remove-installer-packaging/tasks.md`, tasks 2.6/4.2/4.3/5.1/5.4). Verified: all five carry the "Superseded — moved to `05-remove-installer-packaging-windows`" note with correct task-number cross-references.

## 2. Windows verification (moved from `remove-installer-packaging`)

- [x] 2.0 (real bug found during user testing, not in original plan) Fix `resolve_model_dir()`'s incomplete Windows extended-length-path handling

  **Real bug, found and fixed, then confirmed by the user.** User testing (running `transcriber-gui.exe` from `dist/portable/Transcriber/` via its `\\wsl.localhost\...` UNC path) hit `FileNotFoundError: --model-path UNC\wsl.localhost\...\model does not exist` on file transcription. Root cause: `resolve_model_dir()` already stripped Windows' `\\?\C:\...` extended-length prefix (an earlier ctranslate2 fix) but not the UNC form, `\\?\UNC\server\share\...` — stripping only `\\?\` from that form leaves the literal text `UNC\server\share\...`, not a valid path. Fixed in `src-tauri/src/sidecar.rs`: extracted the stripping logic into a pure `strip_extended_length_prefix()` helper handling both forms, added 3 unit tests (local-drive prefix, UNC prefix — using the exact string from the real error — and passthrough for already-plain/non-Windows paths), all passing (`cargo test sidecar::tests`). Rebuilt and reassembled `dist/portable/Transcriber.zip`. **Confirmed fixed**: file transcription works after a full app restart (the first retest reproduced the identical error because the old process was still running in memory — same file on disk, stale in-process code; closing and relaunching resolved it).
- [x] 2.0b (found during the same retest) Add a "Stopping…" UI indicator for live capture

  **Real, minor UX gap, found and fixed.** After 2.0's fix, live capture started correctly (banner/status lines confirmed in the debug log, UI showed active/listening), but clicking Stop gave no visible feedback. Traced it: no `stopping` state indicator existed at all in the frontend (only `starting-stamp`/`live-stamp`) — the transition from active→idle on stop happens almost instantly with nothing shown in between, so it looked like nothing happened even though the stop command itself was working. Added a `stopping-stamp` element (`src/index.html`, `src/main.js`), reusing the existing `stamp-starting` CSS class (visually identical "in progress" style, no new CSS needed) and wiring it into both `setLiveState()` and `updateTopline()` matching the exact pattern the `starting`/`live` stamps already use. Rebuilt, reassembled the zip.
- [ ] 2.0c (raised during the same retest, not yet resolved) Confirm whether "no transcript while talking" was user error or a real bug

  Live capture without checking "include microphone" captures **system audio output** (WASAPI loopback), not the microphone — matching the app's core design (README: "real-time transcription from system audio"). If the user spoke into a mic with mic-capture unchecked and nothing playing through speakers, silence is the correct, working behavior, not a bug. Asked the user to confirm mic-checkbox state and speaker activity; awaiting their answer before deciding whether any further fix is needed here.

- [ ] 2.1 (was 2.6) Run `build_portable.py` on Windows against a `03-add-wsl-windows-build`-produced release dir, extract the produced zip to a fresh path, run it, and confirm it transcribes

  **Partially verified, real GUI drag-and-drop run still needed.** `build_portable.py`'s Windows path already ran for real during `03-add-wsl-windows-build` task 4.1, producing `dist/portable/Transcriber.zip` (264.8MB) with the correct layout. Extracted that zip to a genuinely fresh path (`/tmp/.../win-test/`, not the build output dir) and ran `transcriber-sidecar.exe` directly against it via WSL-to-Windows interop (`chmod +x`, then invoked directly — WSL executes `.exe` PE binaries natively) with `--file <synthetic WAV> --model-path resources/model`: exit 0, correct summary (`Duration: 2.0 seconds`, language detected, `Segments: 0` — correct for a pure tone), transcript file written. This proves the bundled binary+model path from a fresh extraction genuinely works on real Windows (not emulation — `transcriber-sidecar.exe` is a genuine PE32+ binary, confirmed earlier in `03`). **Not verified**: the actual GUI drag-and-drop flow — no GUI-automation tooling available from this WSL shell (no way to see or interact with the Windows desktop). Needs the user's own hands on their Windows desktop.
- [ ] 2.2 (was 4.2) Double-click `transcriber-gui.exe` from Explorer and confirm the app window appears with no console window before, beside, or behind it

  **Not verified — needs the user.** Launched `transcriber-gui.exe` via WSL interop and confirmed the process starts and stays running (didn't crash immediately; `ps`/`kill -0` showed it alive for 5+ seconds until deliberately killed) and created `%LOCALAPPDATA%\com.transcriber.app\EBWebView` (see 2.5 — proof WebView2 actually initialized, not just a silent crash). But whether a console window appears, and what the window looks like, is inherently visual — this WSL shell has no way to see or screenshot the Windows desktop. Genuinely needs the user to double-click it themselves and look.
- [ ] 2.3 (was 4.3) Start a live session and a file transcription and confirm no console window flashes when the sidecar spawns

  **Not verified — needs the user**, same reason as 2.2: visual confirmation of window/console behavior is outside what's checkable from this WSL shell.
- [ ] 2.4 (was 5.1) Extract the zip on a machine that has never had the app installed, run `transcriber-gui.exe`, confirm no admin prompt, no registry writes, and a working transcription

  **Not verified — needs a genuinely clean machine.** This dev machine already has build tooling and prior test state; a real "never had the app installed" check needs different hardware or a clean VM, plus visual confirmation of "no admin prompt" (inherently on-screen). The registry-writes half is at least mechanically checkable (`reg query` before/after) if the user wants to do that themselves alongside the visual check.
- [ ] 2.5 (was 5.4, Windows portion) Confirm the Windows zip leaves no state outside its own folder — deleting the folder is a complete uninstall (note any WebView2 cache location found, matching the pattern `remove-installer-packaging` task 5.4 already documented for Linux/WebKitGTK)

  **Partially verified — WebView2 cache location confirmed for real.** Running `transcriber-gui.exe` (via interop, in 2.2's check) created `%LOCALAPPDATA%\com.transcriber.app\EBWebView` — found via `find` on the Windows-side `AppData\Local`. Matches the exact pattern `remove-installer-packaging` task 5.4 predicted and already documented for Linux/WebKitGTK (standard, unavoidable WebView-engine behavior on any OS, not a registry write, no admin needed). **Not fully verified**: whether anything else is written elsewhere (registry, other AppData locations) needs the user's own machine-level check (e.g. Process Monitor or a before/after registry diff), which isn't accessible from this WSL shell.
