## 1. Clean staging and verification

- [x] 1.1 In `fetch_sidecar_resources.py`, delete the `.cache` directory under the staged model after `download_model()` returns, with `ignore_errors=True` so a future version that stops creating it doesn't break the script (design.md Decision 1). Verify by running the script (`.venv/bin/python fetch_sidecar_resources.py`): it exits 0, prints its staged-model line, and `ls -a src-tauri/resources/model` afterwards shows `model.bin`, `config.json`, `tokenizer.json`, `vocabulary.txt` and `LICENSE.txt` with **no `.cache`**.

  **Verified locally 2026-09-11/12.** `fetch_sidecar_resources.py` now imports `shutil`/`Path` and calls `shutil.rmtree(Path(path) / ".cache", ignore_errors=True)` right after `download_model()`, with a comment naming why (Tauri's `bundle.resources` copies the directory wholesale). It compiles (`py_compile`), and running it exits 0 and prints its staged-model line. `ls -a src-tauri/resources/model` afterwards shows exactly `config.json`, `LICENSE.txt`, `model.bin`, `tokenizer.json`, `vocabulary.txt` — no `.cache`. The `LICENSE.txt` that `licensing` requires beside the weights is untouched.
- [x] 1.2 Build a `.rpm` locally and confirm the bundle is clean (design.md Decision 2): `rpm -qpl` lists no path containing `.cache`, the five model files above are all present under `resources/model/`, and the file count drops by six against the previous build's 27. Then run one offline transcription from that model directory to prove nothing needed was removed.

  **Verified locally 2026-09-11/12.** A `--bundles rpm` build (1m2s, uncompressed payload) produced a package whose `rpm -qpl` has **0** paths containing `.cache`, with all five model files present (`model.bin`, `config.json`, `tokenizer.json`, `vocabulary.txt`, `LICENSE.txt`). An offline transcription through the frozen sidecar against that directory exited 0, logged `Loading base model from src-tauri/resources/model`, and wrote its transcript (empty, since the clip is silence) — so nothing the app needs was removed.

  **Correction to this task's own wording: the drop is eleven files, not six.** A full `comm` against the CI package (27 files) versus this build (16) shows every removed path is under `.cache/huggingface/`: `CACHEDIR.TAG`, `.gitignore`, a `trees/<hash>.json`, and for each of the four model files both a `.lock` and a `.metadata`. The earlier "six" figure came from a narrower grep of the CI listing, which missed the `.metadata` files and the `.gitignore`. Nothing outside `.cache` differs between the two packages.
- [ ] 1.3 On the next tagged run, confirm the CI-built `.rpm` also carries no `.cache` paths — CI is the case that produced the six files in the first place, since it always stages the model from scratch.
