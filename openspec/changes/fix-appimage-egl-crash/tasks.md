## 1. Repack the AppImage in the Linux build

- [x] 1.1 In `build_portable.py`, add a helper that strips the two host-owned libraries from an AppImage and returns the repacked path (design.md Decision 3): read the runtime length from `<image> --appimage-offset`, `dd` that many bytes into a header file, extract the payload with `unsquashfs -o <offset> -d <dir>`, delete `usr/lib/libwayland-client.so.0` and `usr/lib/libwayland-egl.so.1` if present, read the compressor and block size from `unsquashfs -o <offset> -s <image>`, rebuild with `mksquashfs <dir> <payload> -root-owned -noappend -no-xattrs -comp <comp> -b <block>`, concatenate header + payload, and `chmod +x`. Falls back to `zstd`/`131072` when the two fields cannot be parsed. Verify by running it against the released `transcriber_0.1.0_amd64.AppImage` and confirming it exits 0 and produces an executable file.
- [x] 1.2 Wire it into `build_linux()` so the repack happens on the way to `dist/portable/`, in a temporary directory, leaving `src-tauri/target/.../bundle/appimage/` untouched (design.md Decision 5). Verify `python build_portable.py` on a Linux build still prints/returns the `dist/portable/*.AppImage` path, that the bundler's original is byte-identical to before the run, and that running it twice in a row gives the same result (idempotent).
- [x] 1.3 Add the three output assertions, each failing the build (design.md Decision 4): neither library present in the repacked image's listing; final size exactly equals header + payload size; repacked size not more than ~5% above the input. Missing input libraries must **not** fail. Verify each guard actually fires: assert on a deliberately truncated payload, and confirm a second run over an already-stripped image succeeds with no libraries to delete.
- [x] 1.4 Verify the `unsquashfs -o` extraction path end to end, which design.md Decision 3 flags as **untested** — the local proof used the image's own `--appimage-extract`. Confirm the repacked image built via `unsquashfs` runs (window appears, no `EGL_BAD_PARAMETER`), and that `AppRun`, `apprun-hooks/linuxdeploy-plugin-gtk.sh` and the `usr/lib` tree survive extraction with their permissions and executable bits intact.


  **Implemented and verified locally 2026-09-11/12.** All four ticks above are local work; 2.1's note is in section 2, and every CI and hardware half stays open below.

  - **1.1** `build_portable.py` gained `STRIP_FROM_APPIMAGE`, `_squashfs_settings()`, `_verify_stripped()` and `_strip_appimage_libs()`, plus a `tempfile` import. Run against the released `transcriber_0.1.0_amd64.AppImage` it exits 0 in **19.5 s** and produces an executable file. `_squashfs_settings` read **`('zstd', 131072)`** from the input rather than falling back, which is the whole point of Decision 3.
  - **1.2** `build_linux()` now returns `_strip_appimage_libs(matches[0], out_dir / matches[0].name)` instead of `shutil.copy2`. **Idempotent**: two consecutive runs both produced 389,163,512 bytes. **Source untouched**: the bundler's original has the same sha256 before and after. The module docstring's "no assembly needed" line was corrected, since this change makes it false.
  - **1.3** All four guards proven to fire by `test_appimage_strip.py`, which builds a fake AppImage (byte header + real `mksquashfs` payload — the reason `_strip_appimage_libs` takes an optional `offset`): the libraries are removed while `AppRun` and `libgtk-3.so.0` survive; an **already-stripped** input succeeds (absence must not fail the build, Decision 4); a **deliberately truncated** image is rejected with "short write"; an oversized one is rejected as a "compressor mismatch"; and an unreadable image falls back to zstd/128K instead of crashing.
  - **1.4** The `unsquashfs -o` path — flagged **untested** in Decision 3, since the original proof used the image's own `--appimage-extract` — is now verified end to end. The repacked image **runs**: exit 124 (survived the 12 s timeout) with **zero** `EGL_BAD_PARAMETER` lines and no output at all. `apprun-hooks/linuxdeploy-plugin-gtk.sh`, `AppRun` and `AppRun.wrapped` all survived with their exec bits, and 237 `usr/lib` entries remain.

  **The removal is exactly the two libraries, enumerated rather than inferred.** A sorted `comm` of the before/after listings (352 vs 350 entries) shows the only entries gone are `usr/lib/libwayland-client.so.0` and `usr/lib/libwayland-egl.so.1`, with **nothing added** and nothing else changed. Checking the diff rather than trusting the count matters here: an entry-count difference is precisely what produced a wrong "six stray files" figure earlier in this work.

  **Size: 389,163,512 bytes against the original's 389,188,088 — 24,576 bytes smaller**, reproducing the pre-implementation measurement on the `unsquashfs` path too.

  **Correction carried into design.md:** an earlier draft of its Risks section asserted `AppRun` is a symlink in the original. It is a regular 274-byte script. The extractor round-trip concern it raised is still covered by running the repacked image.

## 2. CI

- [x] 2.1 Add `squashfs-tools` to the Linux job's apt install list in `.github/workflows/release.yml` (it provides `mksquashfs`/`unsquashfs`, which the job does not currently have). Verify with `yamllint` plus a YAML parse, and confirm on the next tagged run that the "Assemble portable + CLI artifacts" step does not fail on a missing command.

  **Local half done 2026-09-11/12.** `squashfs-tools` is in the Linux job's apt list, with a comment naming why. The workflow parses (`preflight`, `linux`, `windows`) and the package is present in the "System packages" step. `yamllint` reports only pre-existing 80-column line-length errors (lines 152, 219, 233, 242) — none introduced here, and the repo plainly does not enforce that limit, so unrelated lines were left alone. **Tagged-run half confirmed on run 34694349879:** `squashfs-tools` was already present on the ubuntu-22.04 runner (`1:4.5-3build1`) and the "Assemble portable + CLI artifacts" step completed in ~29 s with no missing-command failure, so the repack ran with the tools it needs.
- [x] 2.2 On the next tagged run, confirm the published AppImage contains neither `libwayland-client.so.0` nor `libwayland-egl.so.1`, and record the published size against the previous run's ~389 MB to confirm the fix stayed size-neutral. CI is the case that matters — it is where the broken artifact was produced.

  **Verified on tagged run 34694349879 (v0.1.0, 2026-09-12).** The published AppImage contains **neither** `libwayland-client.so.0` nor `libwayland-egl.so.1`; the only `libwayland*` entries left are `libwayland-cursor.so.0` and `libwayland-server.so.0`, which Decision 2 deliberately keeps. `squashfs-tools` (4.5-3build1) was already present on the runner, and the whole "Assemble portable + CLI artifacts" step — repack included — took ~29 s (12:48:08.5 → 12:48:37.9).

  **Correction: the fix is NOT size-neutral in CI.** Published size **390,371,832 bytes** against the previous CI AppImage's **389,188,088** (run 34672668438's `ls -la out`) — **+1,183,744 bytes (+1.13 MiB)**, even though the content shrank by 17 entries. Ubuntu 22.04's `mksquashfs` 4.5 evidently compresses slightly worse than appimagetool's built-in at the same zstd/128K settings, which the repack faithfully mirrors. The 5% growth guard passed it at 0.3%.

  **This corrects a figure I reported earlier in this change.** proposal.md and task 1.x record a repack as 24 KB *smaller*; that measurement was made with Fedora's newer `mksquashfs` on this machine, and I initially compared CI's output against my own local repack (389,163,512 bytes, which happened to sit beside the real baseline in `~/Downloads/transcriber-test/`) instead of against CI's previous build. The local numbers stand for local builds; CI costs ~1.1 MiB. Not worth chasing a 0.3% delta by guessing appimagetool's compression level.

## 3. Real-hardware verification

- [ ] 3.1 Download the AppImage from the next tagged run's draft release on the Fedora machine, `chmod +x` it, run it, and confirm the window appears with no `EGL_BAD_PARAMETER` abort — the exact failure this change fixes.
- [ ] 3.2 Transcribe a file through that AppImage and confirm the transcript is written, proving the bundled model and sidecar still resolve after the repack (the model lives in the same `usr/lib` tree the repack rebuilds).
- [ ] 3.3 Tick `01-add-release-pipeline` task 5.3, which this change unblocks, once 3.1 and 3.2 pass.

## 4. Documentation

- [x] 4.1 Confirm README needs no edit: its AppImage row quotes no size (so the size-neutral fix changes nothing) and the FUSE/`--appimage-extract-and-run` note stays accurate. Record the finding rather than assuming it — check the row and the Linux table, and only edit if something is made wrong.

  **Checked and confirmed 2026-09-11/12: no README edit is needed.** Line 34's AppImage row reads "Portable GUI. Run `chmod +x` on it, then run it. AppImages need FUSE (`libfuse2`); without it, run it with `--appimage-extract-and-run`." It quotes no size, so the 24 KB reduction changes nothing, and the FUSE guidance stays accurate: the repack rebuilds the squashfs payload with the input's own zstd/128K settings, so the runtime mounts and self-extracts exactly as before. The other three AppImage mentions are unaffected — line 126 is the `NO_STRIP=1` linuxdeploy note, line 132 names the output path `dist/portable/*.AppImage`, and line 175 is about `--model-path` resolution. Recorded rather than assumed, as the task asked.
- [ ] 4.2 Report the wholesale `copy_tree "$gtk3_libdir"` behavior upstream to `tauri-apps/linuxdeploy-plugin-gtk` (host-owned graphics libraries bundled despite being on the AppImage excludelist), and link the issue here. Optional and explicitly not a dependency of this change.
