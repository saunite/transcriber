# GNU/mingw-w64 cross-compilation target (see README.md's WSL Windows build section).
# Only affects builds targeting x86_64-pc-windows-gnu; native builds on
# each host OS are unaffected.
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
ar = "x86_64-w64-mingw32-ar"
